import React from 'react';

const loading = () => {
    return (
        <div className='flex justify-center items-center w-full h-[80vh] text-2xl font-semibold text-white'>
            <p>Loading......</p>
        </div>
    );
};

export default loading;